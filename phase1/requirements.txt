# Raspberry Pi Action Camera - Phase 1 Dependencies
# Updated to match phase2 architecture

# GPIO Control (replaces deprecated RPi.GPIO with rpi_lgpio)
rpi-lgpio>=0.5.0

# OLED Display Control (replaces LED)
luma.oled>=3.12.0
luma.core>=3.12.0

# Camera Interface
picamera2>=0.3.0

# System Libraries (usually included with Python)
# threading, logging, signal, sys, time, os, enum

# Image Processing (for display font rendering)
Pillow>=8.0.0

# Optional: For development and debugging
# pytest>=6.0.0
# pytest-cov>=2.0.0
